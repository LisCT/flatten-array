import React from 'react';

const Header = () => (
    <header>
        <h1>Flat your Arrays</h1>
        <p> This app will flatten an array of arbitrarily nested arrays of integers into a flat array of integers. e.g. [[1,2,[3]],4] -> [1,2,3,4]</p>
    </header>
)

export default Header;
